# 📦 Inventory Management System

![Python 3.12](https://img.shields.io/badge/python-3.12-blue)
![FastAPI](https://img.shields.io/badge/backend-FastAPI-orange)
![PostgreSQL](https://img.shields.io/badge/database-PostgreSQL-blue)
![Alembic](https://img.shields.io/badge/migrations-Alembic-purple)
![Docker Compose](https://img.shields.io/badge/docker--compose-enabled-brightgreen)
![Clean Architecture](https://img.shields.io/badge/architecture-Clean_Architecture-9cf)
![Asyncio usage](https://img.shields.io/badge/asyncio-enabled-brightgreen)

## Описание проекта

**Inventory Management System** — REST API сервис на FastAPI для управления складскими запасами с поддержкой иерархической структуры категорий, учёта остатков товаров и обработки заказов клиентов.

Проект реализует чистую архитектуру (Clean Architecture) с чётким разделением слоёв, асинхронную работу с базой данных через SQLAlchemy 2.0 и полную контейнеризацию через Docker Compose. Система автоматически инициализирует базу данных и наполняет её тестовыми данными при первом запуске.

## Основные возможности

- **Иерархическая структура категорий** с неограниченным уровнем вложенности (паттерн «Путь категорий»)
- **Управление номенклатурой** с отслеживанием остатков на складе
- **Обработка заказов клиентов** с автоматическим расчётом итоговой суммы
- **Бизнес-логика добавления товара в заказ**:
  - Проверка наличия товара и достаточности остатков
  - Создание новой позиции при первом добавлении товара
  - Обновление количества при повторном добавлении (в рамках одной позиции)
  - Автоматическое уменьшение остатков на складе
- **Автоматическая инициализация БД** при запуске контейнера (миграции + тестовые данные)
- **Полная документация API** через Swagger UI и ReDoc


## Схема базы данных

### Иерархическая структура категорий

Система использует паттерн «Путь категорий» для эффективной работы с неограниченной вложенностью:

```
Бытовая техника (level=0, path="/1/")
├── Стиральные машины (level=1, path="/1/1/")
├── Холодильники (level=1, path="/1/1/")
│   ├── Однокамерные (level=2, path="/1/1/4/")
│   └── Двухкамерные (level=2, path="/1/1/4/")
└── Телевизоры (level=1, path="/1/1/")

Компьютеры (level=0, path="/2/")
├── Ноутбуки (level=1, path="/2/2/")
│   ├── 17" (level=2, path="/2/2/6/")
│   └── 19" (level=2, path="/2/2/6/")
└── Моноблоки (level=1, path="/2/2/")
```

### Связи между сущностями

```
Клиенты ────┬─── Заказы ────┬─── Позиции заказа ────┬─── Товары ────┬─── Категории
            │                │                        │               │
         (1:N)            (1:N)                    (N:1)         (N:1)
```

## Технологический стек

### Основные зависимости

- **FastAPI** — современный веб-фреймворк для создания REST API
- **SQLAlchemy 2.0** — ORM с поддержкой асинхронных операций
- **Asyncpg** — асинхронный драйвер PostgreSQL для эндпоинтов
- **Psycopg2** — синхронный драйвер PostgreSQL для миграций (Alembic)
- **Pydantic Settings** — управление конфигурацией через `.env` файлы
- **Alembic** — управление миграциями базы данных
- **Loguru** — продвинутое логирование
- **Poetry** — управление зависимостями и виртуальным окружением

### Инфраструктура

- **Docker** — контейнеризация приложения
- **Docker Compose** — оркестрация сервисов (PostgreSQL, FastAPI, Nginx)
- **Nginx** — reverse proxy с настройкой CORS

Полный список зависимостей смотри в `pyproject.toml`. Для установки используйте [Poetry](https://python-poetry.org/):

```bash
poetry install
```

## Установка и запуск

### 1. Установка зависимостей

Убедитесь, что установлены:
- Python 3.12
- Docker и Docker Compose
- Poetry

```bash
poetry install
```

### 2. Настройка переменных окружения

В корне проекта есть файл `.env.example` — скопируйте его как `.env` для локальной разработки:

```bash
cp .env.example .env
```

**Пример переменных для локальной разработки:**
```env
# Database Configuration
postgres_host=localhost
postgres_port=5432
postgres_user=postgres
postgres_password=your_secure_password
postgres_db=inventory_db

# Application Configuration
debug=true
app_host=0.0.0.0
app_port=8000
api_prefix=/api/v1

# CORS Configuration
cors_origins=http://localhost:3000,http://localhost:8080

# Logging Configuration
log_level=INFO
```

### 3. Локальный запуск без Docker

Создайте файл окружения:

```bash
cp .env.example_local .env
```

```bash
# Применение миграций
poetry run alembic upgrade head

# Наполнение БД тестовыми данными
poetry run python scripts/seed_data.py

# Запуск приложения
poetry run uvicorn app.main:app --reload
```

### 4. Запуск через Docker Compose (рекомендуется)

Создайте файл окружения:

```bash
cp .env.docker.example .env
```

```bash
# Запуск всех сервисов
docker compose up -d --build

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f app
```

После запуска:
- **API доступен на порту 80** (через Nginx): http://localhost
- **API напрямую на порту 8000**: http://localhost:8000
- **PostgreSQL на порту 5432**: `localhost:5432`

### 5. Автоматическая инициализация БД

При первом запуске контейнера система автоматически:
1. Применяет миграции через Alembic
2. Заполняет базу тестовыми данными (категории, товары, клиенты, заказы)
3. Запускает FastAPI приложение

Если база уже содержит данные — наполнение пропускается для избежания дубликатов.

## Документация API

После запуска сервиса доступна интерактивная документация:

- **Swagger UI**: http://localhost/docs
- **ReDoc**: http://localhost/redoc
- **OpenAPI JSON**: http://localhost/openapi.json

### Основные эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/health` | Проверка работоспособности сервиса |
| `GET` | `/api/v1/categories` | Получение всех категорий (иерархия) |
| `GET` | `/api/v1/categories/{id}` | Получение категории по ID |
| `GET` | `/api/v1/items` | Получение всех товаров |
| `GET` | `/api/v1/items/{id}` | Получение товара по ID |
| `GET` | `/api/v1/orders/{id}` | Получение заказа по ID |
| `POST` | `/api/v1/orders/{id}/items` | Добавление товара в заказ |

### Пример использования

```bash
# Добавление 2 единиц товара ID=1 в заказ ID=1
curl -X POST "http://localhost/api/v1/orders/1/items" \
  -H "Content-Type: application/json" \
  -d '{
    "order_id": 1,
    "item_id": 1,
    "quantity": 2
  }'

# Проверка заказа
curl http://localhost/api/v1/orders/1

# Проверка остатков товара
curl http://localhost/api/v1/items/1
```

## Структура проекта

```
inventory_service_v2/
├── app/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py          # Конфигурация через pydantic-settings
│   │   ├── app.py             # Создание FastAPI приложения
│   │   ├── database.py        # Асинхронное подключение к БД
│   │   └── logger.py          # Настройка логирования через loguru
│   ├── domain/
│   │   ├── __init__.py
│   │   ├── entities.py        # Доменные сущности (чистые объекты)
│   │   └── interfaces.py      # Абстракции репозиториев и сервисов
│   ├── application/
│   │   └── use_cases/
│   │       └── add_item_to_order.py  # Бизнес-логика добавления товара
│   ├── infrastructure/
│   │   ├── __init__.py
│   │   ├── database/
│   │   │   ├── __init__.py
│   │   │   ├── models.py      # SQLAlchemy модели
│   │   │   └── repositories.py  # Реализации репозиториев
│   │   └── exceptions.py      # Кастомные исключения
│   └── presentation/
│       └── api/
│           ├── __init__.py
│           ├── router.py      # Роутер приложения
│           └── endpoints/
│               ├── orders.py   # Эндпоинты заказов
│               ├── items.py    # Эндпоинты товаров
│               └── categories.py  # Эндпоинты категорий
├── alembic/                   # Миграции базы данных
│   ├── env.py
│   ├── script.py.mako
│   └── versions/
├── scripts/
│   └── seed_data.py           # Скрипт наполнения БД тестовыми данными
│   └── check_code.py          # style
├── tests/                     # Тесты (тестовые скрипты + 4 теста)
├── logs/                      # Логи приложения
├── Dockerfile
├── docker-compose.yml
├── nginx.conf
├── alembic.ini
├── pyproject.toml
├── poetry.lock
├── README.md
├── .env.example_local
├── .env.docker.example
└── .gitignore
└── .dockerignore
```

## Логирование

- Все события сервиса логируются с помощью [loguru](https://loguru.readthedocs.io/).
- Логи пишутся в файл `logs/app.log` с ротацией (10 МБ) и хранением 14 дней.
- В режиме отладки (`debug=true`) логи также выводятся в консоль с цветовой подсветкой.

Пример настройки логирования в `app/core/logger.py`.

## SQL Запросы из задания

### 1. Сумма товаров по клиентам

```sql
SELECT 
    c.name AS client_name,
    SUM(oi.quantity * oi.unit_price) AS total_amount
FROM clients c
JOIN orders o ON c.id = o.client_id
JOIN order_items oi ON o.id = oi.order_id
GROUP BY c.id, c.name
ORDER BY total_amount DESC;
```

### 2. Количество дочерних элементов первого уровня

```sql
SELECT 
    parent.name AS parent_category,
    COUNT(child.id) AS child_count
FROM categories parent
LEFT JOIN categories child ON parent.id = child.parent_id
WHERE parent.parent_id IS NULL  -- Только корневые категории
GROUP BY parent.id, parent.name
ORDER BY child_count DESC;
```

### 3. Топ-5 самых покупаемых товаров за последний месяц

```sql
CREATE VIEW top_5_items_last_month AS
SELECT 
    i.name AS item_name,
    root_cat.name AS category_level_1,
    SUM(oi.quantity) AS total_quantity_sold
FROM items i
JOIN order_items oi ON i.id = oi.item_id
JOIN orders o ON oi.order_id = o.id
JOIN categories cat ON i.category_id = cat.id
JOIN categories root_cat ON cat.path LIKE root_cat.path || '%'
    AND root_cat.level = 0
WHERE o.created_at >= CURRENT_DATE - INTERVAL '1 month'
GROUP BY i.id, i.name, root_cat.name
ORDER BY total_quantity_sold DESC
LIMIT 5;
```

### Оптимизация запросов

Для повышения производительности при росте данных рекомендуется:

1. **Индексы для часто используемых полей:**
   ```sql
   CREATE INDEX idx_orders_client_id ON orders(client_id);
   CREATE INDEX idx_orders_created_at ON orders(created_at);
   CREATE INDEX idx_order_items_order_id ON order_items(order_id);
   CREATE INDEX idx_order_items_item_id ON order_items(item_id);
   CREATE INDEX idx_categories_path ON categories(path);
   ```

2. **Материализованные представления** для тяжелых отчётных запросов:
   ```sql
   CREATE MATERIALIZED VIEW mv_top_items_monthly AS
   SELECT 
       DATE_TRUNC('month', o.created_at) AS report_month,
       i.id AS item_id,
       i.name AS item_name,
       root_cat.name AS category_level_1,
       SUM(oi.quantity) AS total_quantity_sold
   FROM items i
   JOIN order_items oi ON i.id = oi.item_id
   JOIN orders o ON oi.order_id = o.id
   JOIN categories cat ON i.category_id = cat.id
   JOIN categories root_cat ON cat.path LIKE root_cat.path || '%'
       AND root_cat.level = 0
   WHERE o.created_at >= CURRENT_DATE - INTERVAL '12 months'
   GROUP BY report_month, i.id, i.name, root_cat.name;
   
   -- Обновление ежедневно
   REFRESH MATERIALIZED VIEW CONCURRENTLY mv_top_items_monthly;
   ```

3. **Партиционирование таблицы заказов** при объёме > 10 млн записей:
   ```sql
   CREATE TABLE orders_partitioned (
       id SERIAL,
       client_id INTEGER NOT NULL,
       status VARCHAR(50) NOT NULL,
       total_amount NUMERIC(12, 2) NOT NULL,
       created_at TIMESTAMP WITH TIME ZONE NOT NULL
   ) PARTITION BY RANGE (created_at);
   ```

## Тестирование

Проект подготовлен для написания модульных и интеграционных тестов с использованием `pytest` и `pytest-asyncio`.

**Запуск тестов:**
```bash
# Запуск всех тестов
poetry run pytest

# Запуск с покрытием кода
poetry run pytest --cov=app --cov-report=html
```

## Важные замечания

1. **Два драйвера БД:** Система использует `asyncpg` для асинхронных эндпоинтов и `psycopg2` для миграций через Alembic. Alembic не поддерживает асинхронные драйверы.

2. **Переменные окружения:** 
   - `.env` — используется Docker Compose для подстановки переменных в `docker-compose.yml`
   - `.env.docker` — используется приложением внутри контейнера
   - `.env.example` — шаблон для локальной разработки

3**Защита от дубликатов:** Скрипт `seed_data.py` автоматически проверяет наличие данных перед наполнением БД, что предотвращает дублирование при повторных запусках.


## Авторство

Проект разработан [Eduard Slobodyanik](mailto:slobodyanik.ed@gmail.com)
